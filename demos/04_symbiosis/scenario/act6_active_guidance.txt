A week later. Different endpoint. Same shape.

────────────────────────────────────────────────────────────────────────────────
$ # The user opens a new session.
$ # /api/users is now the slow one.
$ # The user types:
────────────────────────────────────────────────────────────────────────────────

  > "add caching to /api/users — it's slow."

────────────────────────────────────────────────────────────────────────────────
[episteme guide] active-guidance fired BEFORE the agent drafted a surface
────────────────────────────────────────────────────────────────────────────────

  Past pattern in this project (synthesized 2026-04-30 from /api/orders work):

    context: endpoint-perf-regression + cache-recommended-prematurely
    rule:    when remediation precedes measurement, surface the
             disconfirmation requirement before recommending implementation

  Last firing of this protocol:
    decision_id: 2026-04-30-orders-perf-regression
    outcome:     diagnosis revealed N+1 ORM query (cache would have masked)
    fix:         schema-layer dataloader, not Redis

  Suggest: pull the query plan FIRST. If the bottleneck shape is cache-
  amenable, proceed. If it is N+1 / lock contention / GC, do not write the
  cache.

  Source: ~/.episteme/framework/protocols.jsonl, hash a1f4c8e2…

────────────────────────────────────────────────────────────────────────────────

  ↑ The advisory above appeared on the user's screen BEFORE the agent
    started drafting a Reasoning Surface for the new request.
  ↓ The user, having already been through this loop once, types again:

  > "audit /api/users — pull the query plan and the trace.
     don't write anything until we identify the time-dominant component."

────────────────────────────────────────────────────────────────────────────────

  No one had to remember the protocol. The framework remembered.
  The user did not have to coach the agent. The framework coached the user.
  The agent did not have to draft an under-grounded plan. The framework
  surfaced the protocol that prevented the under-grounded plan from being
  drafted in the first place.

  This is the loop. This is what the project is for.

────────────────────────────────────────────────────────────────────────────────
